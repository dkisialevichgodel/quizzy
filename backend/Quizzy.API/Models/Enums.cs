namespace Quizzy.API.Models;

public enum UserRole
{
    User = 0,
    Admin = 1
}

public enum Difficulty
{
    Easy = 0,
    Medium = 1,
    Hard = 2
}

public enum QuestionType
{
    MultipleChoice = 0,
    OpenText = 1
}
